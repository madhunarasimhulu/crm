import React from 'react';
import { PageWrapper, CustomerSearchByCardNumber } from '../../components';

const SearchByCardNumber = () => (
  <PageWrapper>
    <CustomerSearchByCardNumber />
  </PageWrapper>
);

export default SearchByCardNumber;
